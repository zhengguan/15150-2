structure SeqPractice =
struct
  type 'a seq = 'a Seq.seq

  (* Task 8.1 *)
  (* prefixSum : int seq -> int seq
   * REQUIRES:
   * ENSURES:
   *)
  fun prefixSum (s: int seq) : int seq = raise Fail "unimplemented"

  (* Task 8.2 *)
  (* concatInt : int seq -> string
   * REQUIRES:
   * ENSURES:
   *)
  fun concatInt (s: int seq) : string = raise Fail "unimplemented"
end